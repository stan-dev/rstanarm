# formula method for stanreg objects

formula method for stanreg objects

## Usage

``` r
# S3 method for class 'stanreg'
formula(x, ..., m = NULL)
```

## Arguments

- x:

  A stanreg object.

- ...:

  Can contain `fixed.only` and `random.only` arguments that both default
  to `FALSE`.
