#' @param ... Further arguments passed to the function in the \pkg{rstan} 
#'   package (\code{\link[rstan]{sampling}}, \code{\link[rstan]{optimizing}},
#'   or \code{\link[rstan]{vb}}) corresponding to the estimation method named by
#'   \code{algorithm}. For example, if \code{algorithm='sampling'} we might 
#'   specify \code{iter}, \code{chains}, \code{cores}, etc.).
